workflows:
  ios-release:
    name: iOS TestFlight Release
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: Codemagic
    environment:
      node: latest
      groups:
        - app_store_credentials
      vars:
        BUNDLE_ID: "com.wanderly.fogofwar"
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    scripts:
      - name: Set up code signing (fetch profiles from Apple)
        script: |
          keychain initialize
          app-store-connect fetch-signing-files "com.wanderly.fogofwar" \
            --type IOS_APP_STORE \
            --create
          keychain add-certificates
      - name: Install pnpm & dependencies
        script: |
          npm install -g pnpm@9 --force
          pnpm install --no-frozen-lockfile
      - name: Expo Prebuild (Generate iOS Native Project)
        script: |
          cd artifacts/wanderly
          npx expo prebuild --platform ios --clean --no-install
          cd ios && pod install
      - name: Set up code signing settings on Xcode project
        script: |
          cd artifacts/wanderly/ios
          xcode-project use-profiles
      - name: Build IPA for App Store
        script: |
          cd artifacts/wanderly/ios
          xcode-project build-ipa \
            --workspace "$(find . -maxdepth 1 -name '*.xcworkspace' | head -n 1)" \
            --scheme "$(find . -maxdepth 1 -name '*.xcworkspace' | sed 's/\.xcworkspace//' | sed 's/^\.\///')" \
            --config Release
    artifacts:
      - artifacts/wanderly/ios/build/remote-build/Build/Products/Release-iphoneos/*.ipa
      - artifacts/wanderly/dist/**/*
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
